# Example environment variables for TippingChain Example Project
# Replace these placeholder values with your actual credentials and configuration

# Client ID for Thirdweb or SDK authentication
CLIENT_ID=placeholder-client-id

# Secret Key for Thirdweb client (if required)
SECRET_KEY=placeholder-secret-key

# Relay API endpoint for blockchain interactions
RELAY_API_URL=https://api.relay.link

# Chain ID for the blockchain network (e.g., Base chain ID)
CHAIN_ID=8453

# Contract address for the tipping smart contract
CONTRACT_ADDRESS=placeholder-contract-address

# Environment setting (e.g., 'production' or 'testnet')
ENVIRONMENT=production
